[app]
name = "secrets-v2"
version = "0.1.0"
description = "Manage environment variable secrets for other apps (services_v2 interface)"

[runtime.container]
image = "Dockerfile"
port = 8080

[resources]
memory_mb = 256
cpu_millicores = 1000

[data]
sqlite = ["main"]

[[services.v2.provides]]
service = "github.com/imbue-openhost/openhost/services/secrets"
version = "0.1.0"
endpoint = "/_service_v2/"
